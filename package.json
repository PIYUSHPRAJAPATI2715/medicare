{
  "name": "medicare-plus-monorepo",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "cd web-portal && npm install && npm run build",
    "start": "cd backend && npm start"
  }
}
